import { ClassStatusPipe } from './class-status.pipe';

describe('ClassStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ClassStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
